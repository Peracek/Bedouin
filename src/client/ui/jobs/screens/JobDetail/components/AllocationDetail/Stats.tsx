import React from 'react'

import AllocationStats from '@shared/types/AllocationStats'

const Stats = (props: { stats: AllocationStats }) => {
  const { stats } = props
  return (
    <div></div>
  )
}

export default Stats